{
  "name": "sticker-book",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && yarn run watch\" \"cd client && yarn start\"",
    "install": "cd server && yarn && cd ../client && yarn",
    "seed": "cd server && yarn run seed",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && yarn run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.1.0",
    "faker": "5.5.3",
    "graphql": "^16.5.0",
    "react-router-dom": "^6.3.0",
    "tailwind": "^4.0.0",
    "yarn": "^1.22.18"
  },
  "devDependencies": {
    "tailwindcss": "^3.0.24"
  }
}
